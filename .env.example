# Sign in with Chutes - Environment Variables Template
# Copy this file to .env.local and fill in your values

# ==============================================================================
# Required: OAuth Client Credentials
# ==============================================================================
# Get these by running: npx tsx scripts/setup-chutes-app.ts
# Or register manually at: https://api.chutes.ai/idp/apps

CHUTES_OAUTH_CLIENT_ID=cid_your_client_id_here
CHUTES_OAUTH_CLIENT_SECRET=csc_your_client_secret_here

# ==============================================================================
# Optional: OAuth Configuration
# ==============================================================================

# Scopes to request (space-separated)
# Default: "openid profile chutes:invoke"
# Available: openid, profile, chutes:invoke, account:read, billing:read
CHUTES_OAUTH_SCOPES="openid profile chutes:invoke"

# Explicit redirect URI (auto-detected if not set)
# CHUTES_OAUTH_REDIRECT_URI=https://yourapp.com/api/auth/chutes/callback

# App URL used for redirect URI construction
# Default: auto-detected from request
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Override IDP base URL (rarely needed)
# CHUTES_IDP_BASE_URL=https://api.chutes.ai
